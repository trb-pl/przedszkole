---
import { Image } from 'astro:assets';
import heroImage from '~/assets/images/hero-koloroweprzedszkole.jpg';
import koloroweLogo from '~/assets/images/kolorowe.svg?raw';
---

<section class="relative min-h-screen overflow-hidden">
  <!-- Background image -->
  <Image
    src={heroImage}
    alt="Kolorowe Przedszkole — Saska Kępa, Warszawa od 1991 roku"
    class="absolute inset-0 w-full h-full object-cover"
    loading="eager"
    fetchpriority="high"
  />

  <!-- Overlay -->
  <div class="absolute inset-0 bg-black/25"></div>

  <!-- Content -->
  <div class="relative z-10 flex flex-col items-center justify-center h-screen px-6 gap-8 text-center text-brand-cream">

    <!-- Typography composition -->
    <div class="flex flex-col items-center gap-1 sm:gap-2">
      <div class="w-[60vw] sm:w-[55vw] md:w-[50vw] lg:w-[45vw] max-w-[700px]" set:html={koloroweLogo} />
      <h1 class="font-sans font-bold uppercase text-[clamp(3rem,14vw,12rem)] leading-none tracking-wider text-brand-cream">
        PRZEDSZKOLE
      </h1>
    </div>

    <!-- Tagline -->
    <p class="font-sans text-2xl sm:text-3xl md:text-4xl text-brand-cream/95 max-w-3xl mt-8">
      Budujemy bezpieczny pomost między domem a szkołą.
    </p>

    <!-- Sub-tagline -->
    <p class="font-sans text-base sm:text-lg uppercase tracking-[0.3em] text-brand-cream/80">
      od 1991 · Saska Kępa · Warszawa
    </p>

    <!-- CTA -->
    <a
      href="#olga"
      class="inline-block bg-brand-yellow text-brand-navy font-sans font-bold uppercase tracking-wider px-8 py-4 rounded-full transition-colors duration-200 hover:bg-brand-cream hover:text-brand-navy mt-4"
    >
      Poznaj nas
    </a>

  </div>
</section>
